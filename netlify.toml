[build]
  command = "npm run build"
  publish = "build"

# Optional: specify directory where Netlify functions generated by the adapter will live
# functions = ".netlify/functions"

[dev]
  command = "npm run dev"
